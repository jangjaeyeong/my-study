1. 관계형 데이터베이스(RDB)
	RDB는 정형 데이터베이스로서 테이블 형태로 데이터를 관리한다.
	규격이나 타입이 엄격하게 관리되며 JOIN이라는 키워드로 테이블과 테이블을 매핑
	할 수 있다는 특징도 함께 가지고 있다.

2. NoSQL
	NoSQL은 비관계형 데이터베이스로 엄격한 테이블 구조를 따르지 않는다.
	주로 대용량 데이터의 분산 처리와 유연한 스키마가 필요한 애플리케이션에 사용이 	되며 다양한 데이터 모델을 사용한다.

	NoSQL의 데이터 모델
	1. key-value store 
		key-value의 한 쌍으로 데이터를 저장하는 방식 (Redis)
	2. Document store
		Json이나 XML같은 문서 형태로 데이터를 저장하는 방식
		회원 정보 등을 관리할 때 주로 사용한다 (MongoDB)
	3. Graph 
		노드와 엣지 형태로 데이터를 저장하는 방식
		그래프 탐색 기능을 제공한다. (Neo 4J)
	4. WideColumn store
		데이터를 하나의 열로 묶어서 저장 
		배열과 비슷한 형태를 띄며 테이블과 가장 유사한 모습을 띄지만
		테이블과는 다르게 레코드 별로 컬럼의 갯수나 형태가 다르게 필요한 정보만
		저장할 수 있다. (Apach Cassandra)
